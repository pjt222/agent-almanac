{
  "name": "skillnet-viz",
  "version": "1.0.0",
  "private": true,
  "description": "Cyberpunk Skillnet Visualization for agent-almanac",
  "type": "module",
  "scripts": {
    "pipeline": "bash build.sh",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build-data": "node build-data.js",
    "build-manifest": "node build-icon-manifest.js",
    "build-favicon": "Rscript build-favicon.R",
    "build-workflow": "Rscript build-workflow.R",
    "test": "vitest run",
    "validate-locales": "node scripts/validate-locales.js"
  },
  "dependencies": {
    "3d-force-graph": "^1",
    "d3": "^7",
    "force-graph": "^1.51.4",
    "mermaid": "^12.0.0",
    "three": "^0.186.0"
  },
  "devDependencies": {
    "js-yaml": "^5.4.1",
    "sharp": "^0.35.4",
    "vite": "^8",
    "vitest": "^5.0.1"
  },
  "//": "overrides.lodash-es: mermaid 12.0.0 depends on chevrotain ~11.1.2, which pins lodash-es to exactly 4.17.23 (GHSA-r5fr-rjxr-66jc, GHSA-f23m-r3pf-42rh). Drop the override once mermaid depends on chevrotain >=12, which has no lodash-es dependency.",
  "overrides": {
    "lodash-es": "^4.18.1"
  }
}
