# Template for the gitignored `.env`. Copy, then fill in values:
#   cp .env.example .env    # .env is gitignored; .env.example is tracked and must stay empty of values
#
# Nothing in this repository reads either file. Keys here are used only while AUTHORING
# content that documents a third-party service, to upgrade a documented claim into a
# measured one. A skill, script, test or CI gate that required a key could not run for a
# contributor who has none, so none of them may read this.

# TypeSafe AI (System One / Jev) — https://docs.typesafe.ai
TYPESAFE_API_KEY=
